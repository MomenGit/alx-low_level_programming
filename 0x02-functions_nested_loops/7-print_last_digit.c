#include "main.h"

/**
 * print_last_digit - returns the last digit of a number
 * @n: input number
 *
 * Return: On success n % 10.
 * On error, .
 */
int print_last_digit(int n)
{
	return (n % 10);
}
