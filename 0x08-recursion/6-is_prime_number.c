/**
 * is_prime_number - checks if a number is prime
 * @n: number to be checked
 *
 * Return: On success 1 otherwise 0.
 * On error, .
 */
int div(int n, int lastDiv)
{
	if (!(n % lastDiv))
		return 0;
}
int is_prime_number(int n)
{
	if (n / 2)
		is_prime_number(n);
}
