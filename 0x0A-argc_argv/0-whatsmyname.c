#include <stdio.h>
/* more headers goes there */

/**
 * main - Entry point
 * prints program name
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
