#include "main.h"

/**
 * mul - returns the multiplication of two integers
 * @a: first input integer
 * @b: second input integer
 *
 * Return: On success .
 * On error, .
 */
int mul(int a, int b)
{
	return (a * b);
}
